numbers = [2, 3, 4, 5, 6]
prod_pairs = []
for i in range((len(numbers) + 1) // 2):
    prod = numbers[i] * numbers[len(numbers) - 1 - i]
    prod_pairs.append(prod)
print(f'{numbers} список произведений пар -> {prod_pairs}')